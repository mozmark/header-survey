This is an attempt at a simple header survey script. It expects a host list with lines of the form rank,host:

e.g:
1,google.com
2,facebook.com
...etc

You can get a host list from Alexa (http://www.alexa.com/topsites).

run like this:
./header-survey.py hostlist
